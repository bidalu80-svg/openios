diff --git a/fs/sock.c b/fs/sock.c
--- a/fs/sock.c
+++ b/fs/sock.c
@@ -620,7 +620,7 @@ int_t sys_recvfrom(fd_t sock_fd, addr_t buffer_addr, dword_t len, dword_t flags,
         free(buffer);
         return errno_map();
     }

-    if (user_write(buffer_addr, buffer, len)) {
+    if (user_write(buffer_addr, buffer, res)) {
         free(buffer);
         return _EFAULT;
     }
